---
/**
 * FinalCTA — the last surface a scrolling user sees.
 *
 * Rule: only one call to action per final CTA section. We give two
 * buttons but the secondary ("Watch demo") is visually muted so the
 * eye lands on "Download for Windows".
 *
 * Background is a stylised aurora glow that implies the app's identity
 * without re-using the hero's exact composition.
 */
import { Download, Play, ArrowRight } from 'lucide-react';
---

<section id="download" class="relative isolate overflow-hidden py-24 md:py-36">
  <div class="absolute inset-0 -z-10" aria-hidden="true">
    <div
      class="aurora-blob animate-aurora"
      style="width: 90vw; height: 90vw; left: 5vw; top: -40vw; background: radial-gradient(circle, rgba(167,139,250,0.55), transparent 55%);"
    ></div>
  </div>

  <div class="container-narrow px-4 text-center md:px-6">
    <h2 class="text-display font-semibold text-white">
      <span data-i18n-lang="en">Ready to <span class="text-gradient">never type again</span>?</span>
      <span data-i18n-lang="fr">Prêt à <span class="text-gradient">ne plus jamais taper</span> ?</span>
    </h2>
    <p class="mx-auto mt-4 max-w-xl text-ink-300 md:text-lg">
      <span data-i18n-lang="en">Free forever. No credit card to install. Upgrade when the Free cap starts cramping your style.</span>
      <span data-i18n-lang="fr">Gratuit à vie. Aucune carte pour installer. Passez Pro quand la limite Free vous ralentit.</span>
    </p>
    <div class="mt-10 flex flex-col items-center justify-center gap-3 sm:flex-row">
      <a href="https://github.com/mobel8/parlys/releases/latest" class="btn-primary focus-ring animate-pulse-glow">
        <Download size={18} aria-hidden="true" />
        <span data-i18n-lang="en">Download for Windows</span>
        <span data-i18n-lang="fr">Télécharger pour Windows</span>
        <ArrowRight size={16} className="opacity-80" />
      </a>
      <a href="#demo" class="btn-secondary focus-ring">
        <Play size={16} aria-hidden="true" />
        <span data-i18n-lang="en">Watch 60 s demo</span>
        <span data-i18n-lang="fr">Voir la démo 60 s</span>
      </a>
    </div>

    <div class="mt-10 grid grid-cols-1 gap-4 sm:grid-cols-3">
      {[
        { k: '< 400 ms', vEn: 'voice-to-voice latency',  vFr: 'latence voix-à-voix' },
        { k: '30+',      vEn: 'languages supported',      vFr: 'langues supportées' },
        { k: '0 €',      vEn: 'to get started',           vFr: 'pour démarrer' },
      ].map(s => (
        <div class="glass rounded-2xl px-4 py-4">
          <div class="font-display text-2xl font-semibold text-white">{s.k}</div>
          <div class="text-xs text-ink-400">
            <span data-i18n-lang="en">{s.vEn}</span>
            <span data-i18n-lang="fr">{s.vFr}</span>
          </div>
        </div>
      ))}
    </div>
  </div>
</section>
